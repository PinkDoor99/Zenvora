services:
  - type: web
    name: zenvora-api
    env: node
    plan: starter
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: "5000"
      - key: VERCEL_URL
        sync: false
